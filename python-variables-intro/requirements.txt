# This project does not require any specific third-party Python packages.
# It uses only standard built-in Python features.
# If this were a larger project, specific package dependencies would be listed here,
# e.g.,
# requests==2.28.1
# numpy==1.23.2