# Spring Boot application configuration properties.
# This file is intentionally minimal to keep the demo focused on GenAI documentation.

# Server port for the Spring Boot application
server.port=8080

# Application name, can be used for logging or monitoring
spring.application.name=docugenius-demo-app

# Example of a custom property that could be documented by GenAI
app.feature.enabled=true
app.data.source=in-memory